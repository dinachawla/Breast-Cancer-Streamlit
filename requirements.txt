# --- UI layer
streamlit==1.31.0          # latest release that still supports NumPy 1.x

# --- model + utilities
scikit-learn==1.3.2        # works with your pickled Pipeline
joblib==1.3.2

# --- numeric stack (pin to NumPy < 2)
numpy==1.26.4
pandas==2.2.2              # newest pandas that still runs on NumPy 1.x
